name: inventory_comsitec
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  audioplayers: ^6.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.0
  mobile_scanner: ^3.0.0
  path_provider: ^2.0.10
  share_plus: ^10.0.2
  open_file: ^4.0.3
  intl: ^0.18.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/beep.mp3
